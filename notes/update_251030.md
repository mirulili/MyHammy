# Update Notes: 2025-10-30

## Key Changes
*   **Logic Fixes**: Improved state management in `src/analysis/wheel_spin.py` and `src/analysis/drinking.py`. Changed state variables (`total_wheel_time`, `movement_start_time`) from global/local variables to function arguments and return values. Ensured that state is correctly maintained between function calls.
*   **Essential Module Imports**: Added `datetime` module to `src/data/database.py` and `cv2` module to `src/analysis/drinking.py`. (Managed dependencies)
*   **Added `requirements.txt`**: Created `requirements.txt` file containing `opencv-python`.

## Issues & Resolutions
*   **`ModuleNotFoundError: No module named 'analysis'`**: Fixed typo.
*   **macOS Camera Warning (`AVCaptureDeviceTypeExternal is deprecated`)**: Decided to ignore it for now as it does not affect the program's functionality. (warning from the `cv2` library using an older macOS camera API)
